version: 2

models:
    - name: staging_aws_twitter_data_table
      description: Staging table for twitter data
      columns:
        - name: tweet
          tests:
            - not_null
        - name: pos
          description: test that the sentiment analysis hasn't fkd up
          tests:
            - not_null