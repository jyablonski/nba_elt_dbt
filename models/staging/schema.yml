version: 2

models:
  - name: stg_aws_adv_stats_table
    description: Staging table for advanced stats
    tags: [NBA PRODUCTION - STAGING]
    columns:
      - name: team
        tests:
          - unique
          - not_null
  - name: stg_aws_boxscores_table
    description: Staging table for box scores
    tags: [NBA PRODUCTION - STAGING]
    columns:
      - name: player
        tests:
          - not_null
  - name: stg_aws_contracts_table
    description: Staging table for box scores
    tags: [NBA PRODUCTION - STAGING]
    columns:
      - name: player
        tests:
          - not_null
      - name: team
        tests:
          - not_null
      - name: salary
        tests:
          - not_null
  - name: stg_aws_injury_data_table
    description: Staging table for box scores
    tags: [NBA PRODUCTION - STAGING]
    columns:
      - name: player
        tests:
          - not_null
      - name: team
        tests:
          - not_null
      - name: injury
        tests:
          - not_null
      - name: description
        tests:
          - not_null
  - name: stg_aws_odds_table
    description: Staging table for box scores
    tags: [NBA PRODUCTION - STAGING]
    columns:
      - name: team
        tests:
          - not_null
      - name: date
        tests:
          - not_null
      - name: time
        tests:
          - not_null
  - name: stg_aws_reddit_data_table
    description: Staging table for box scores
    tags: [NBA PRODUCTION - STAGING]
    columns:
      - name: scrape_date
        tests:
          - not_null
      - name: scrape_time
        tests:
          - not_null
  - name: stg_aws_schedule_table
    description: Staging table for box scores
    tags: [NBA PRODUCTION - STAGING]
    columns:
      - name: away_team
        tests:
          - not_null
      - name: home_team
        tests:
          - not_null
      - name: proper_date
        tests:
          - not_null
  - name: stg_aws_stats_table
    description: Staging table for box scores
    tags: [NBA PRODUCTION - STAGING]
    columns:
      - name: player
        tests:
          - not_null
  - name: stg_aws_transactions_table
    description: Staging table for box scores
    tags: [NBA PRODUCTION - STAGING]
    columns:
      - name: date
        tests:
          - not_null
      - name: transaction
        tests:
          - not_null