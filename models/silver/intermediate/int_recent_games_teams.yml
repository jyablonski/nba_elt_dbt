version: 2

models:
  - name: int_recent_games_teams
    description: '{{ doc("int_recent_games_teams")}}'
    data_tests:
      - dbt_expectations.expect_compound_columns_to_be_unique:
          arguments:
            column_list: ["team", "game_date"]
