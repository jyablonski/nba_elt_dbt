version: 2

sources:
  - name: gold
    database: jacob_db
    schema: gold
    description: Gold-layer application and operations tables maintained outside dbt.
    tables:
      - name: ingestion_freshness_history
        description: |
          Legacy append-only audit table from the prior `ingestion_freshness` post_hook.
          Queried via dbt model `ingestion_freshness_history_view`, not materialized over.
