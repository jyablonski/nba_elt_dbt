version: 2

sources:
  - name: gold
    database: jacob_db
    schema: gold
    tables:
      - name: ingestion_freshness_history
        description: |
          Append-only audit table (bootstrap + `ingestion_freshness` post_hook).
          Queried via dbt model `ingestion_freshness_history_view`, not materialized over.
