version: 2

models:
  - name: team_record_daily_rollup
    columns:
      - name: team
        tests:
          - not_null
      - name: game_date
        tests:
          - not_null
      - name: running_total_games_played
        tests:
          - not_null